#include<iostream>
#include<cmath>

int main(){
    double x=2.01;
    double y=4;
    double z;
    
    // z = std::max(x,y);
    //  z = std::min(x,y);

    // z=pow(2,4);
    // z=sqrt(19);
    // z=abs(-43);
    // z =round(x);
    // z=ceil(x);
    // z=floor(x)

    std::cout << z;


    return 0;
}